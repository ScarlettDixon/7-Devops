---
- name: Converge
  hosts: all
  tasks:
    - name: "Include Role" 
      include_role:
        name: "setup"
  # gather_facts: false
#- name: "Importing Test PLaybook"
  #import_playbook: "../../roles/setup/tasks/connection.yml"
  #import_playbook: "../../test-server.yml"
  # tasks:
  #   - name: Replace this task with one that validates your content
  #     ansible.builtin.debug:
  #       msg: "This is the effective test"
